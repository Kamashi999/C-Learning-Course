

string[] names =
{
    "Anna", "Mark"
};

// Pętla foreach, działa na zasadzie stowrzenia jej własnej zmiennej, z której czyta wartości innej zmiennej.

foreach (string name in names)
{
    Console.WriteLine(name);
}

